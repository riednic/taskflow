meta {
  name: 6 - List Tasks - sorted by priority desc (200)
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/tasks?sort=priority,desc
  body: none
  auth: bearer
}

params:query {
  sort: priority,desc
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("returns 200", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.body.content).to.be.an("array");
  });
}
