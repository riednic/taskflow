meta {
  name: 8 - List Users - unauthenticated (401)
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/v1/users
  body: none
  auth: none
}

tests {
  test("returns 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
