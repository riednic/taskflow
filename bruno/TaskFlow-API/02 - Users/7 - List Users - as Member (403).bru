meta {
  name: 7 - List Users - as Member (403)
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/v1/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{memberToken}}
}

tests {
  test("returns 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
