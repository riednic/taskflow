meta {
  name: 2 - Create User - as Member (403)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{memberToken}}
}

body:json {
  {
    "name": "Should Not Be Created",
    "email": "should-not-exist@taskflow.local",
    "password": "SomePassword123!",
    "role": "MEMBER"
  }
}

tests {
  test("returns 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
