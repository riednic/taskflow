meta {
  name: 5 - Logout - revoked token rejected (401)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/statistics/tasks
  body: none
  auth: bearer
}

auth:bearer {
  token: {{logoutTestToken}}
}

tests {
  test("returns 401 because the token was blacklisted on logout", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
