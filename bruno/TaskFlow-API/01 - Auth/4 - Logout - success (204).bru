meta {
  name: 4 - Logout - success (204)
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{logoutTestToken}}
}

tests {
  test("returns 204", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
